#include <iostream>
#include <stdio.h>
#include <string>
#include <fstream>

#include "dnabitset.h"

int main(int argc, char **argv) {
  const char *filename = argv[1];
  std::cout << "Filename: " << filename << "\n";

  return 0;
}
